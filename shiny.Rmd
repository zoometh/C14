---
runtime: shiny
output: html_document
---
  
```{r}
library(shiny)
print(getwd())
ui <- shinyUI(
  fluidPage(
    includeHTML(rmarkdown::render("index_1.Rmd"))
  )
)
server <- function(input, output) { }

shinyApp(ui, server) 
```

